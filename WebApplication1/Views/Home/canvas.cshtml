
<!DOCTYPE html>
<html>
<head>
    <title></title>
	<meta charset="utf-8" />
  
</head>
<body>
    <canvas id="myCanvas" width="1140" height="600" style="border:1px solid #d3d3d3;">
        Your browser does not support the HTML5 canvas tag.
    </canvas>

    <script>

    var canvas = document.getElementById("myCanvas");
    var c = canvas.getContext("2d");
    c.fillStyle = "#000000";
    c.fillRect(0, 0, canvas.width, canvas.height);


    c.strokeStyle = "#FFFFFF";
    c.lineWidth = 20;
    c.strokeRect(25, 25, (canvas.width - 50), (canvas.height - 50));

    c.font = 'normal 70px Arial';
    c.fillStyle = "#FFFFFF";
    c.fillText('Thanks ', 100, 150);
   
    c.font = 'normal 40px Arial';
    c.fillText('Gavriel Sela:  gavriel.sl@gmail.com !', 100, 250);

    c.fillStyle = "#FF0000";

         c.beginPath();
         c.arc((canvas.width - 100), 150, 60, 0, 2 * Math.PI);
      //     c.arc((canvas.width - 300), 250, 50, 0, 2 * Math.PI);
         c.arc((canvas.width - 175), 100, 50, 0, 2 * Math.PI);
         c.fill();
         c.closePath();

         var pX = 0;
         setInterval(function () {
           
             pX = pX + 1;
             c.fillStyle = "rgba(0,0,0,0.03)";
          c.fillRect(0, 0, canvas.width, canvas.height);
           
             c.beginPath();             
             c.fillStyle = "white";
             c.arc(pX, 400, 60, 0, Math.PI * 2);
             c.stroke();
             c.fill();

         }, 10);
   
    //c.strokerect(10, 10, (canvas.width - 20), (canvas.height - 20));
    </script>


    @*<canvas id="myCanvas" width="900" height="400">gooo</canvas>
    <script>

        window.onload() = function () {

            var canvas = document.getElementById("myCanvas"),
                c = canvas.getContext("2d");
        c.fillStyle = "black";
            c.fillRect(0, 0, canvas.width, canvas.hight);

            c.fillStyle="white";
            c.fillRect(15, 15, 10, 50);
        };
    </script>*@
</body>
</html>
